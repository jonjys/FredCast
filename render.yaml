services:
  - type: web
    name: fredcast-relay
    runtime: node
    plan: free
    rootDir: relay
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
